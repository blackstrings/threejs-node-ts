import { BaseLoader } from './BaseLoader';

/**
 * Handles loading OBJ files or 3d files.
 * This class is pretty much a wrapper around the three loaders
 */
export class OBJLoaderWrapper extends BaseLoader {
  constructor() {
    super();
  }
}
